<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>SoftCampus - Biblioteca Educativa</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <script src="https://unpkg.com/lucide@latest"></script>
    <style>
        @keyframes fadeIn { from { opacity: 0; } to { opacity: 1; } }
        #app-container, #login-container { animation: fadeIn 0.4s ease-out; }
        @keyframes splashPop { 0% { opacity: 0; transform: scale(0.6) rotate(-8deg); } 70% { opacity: 1; transform: scale(1.08) rotate(2deg); } 100% { opacity: 1; transform: scale(1) rotate(0deg); } }
        .splash-pantalla { position: fixed; inset: 0; background-color: rgb(15 23 42); display: flex; flex-direction: column; align-items: center; justify-content: center; z-index: 60; }
        .splash-logo { animation: splashPop 0.7s cubic-bezier(0.34, 1.56, 0.64, 1) both; }
        .splash-pantalla.splash-oculto { opacity: 0; transition: opacity 0.5s ease-out; pointer-events: none; }
    </style>
</head>
<body class="bg-slate-900 text-slate-100 min-h-screen flex flex-col font-sans">

    <!-- Pantalla Splash -->
    <div id="splash-screen" class="splash-pantalla">
        <div class="splash-logo bg-blue-600 p-5 rounded-2xl text-white mb-4">
            <svg xmlns="http://www.w3.org/2000/svg" width="40" height="40" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><rect width="20" height="14" x="2" y="3" rx="2"/><line x1="2" x2="22" y1="21" y2="21"/></svg>
        </div>
        <h1 class="text-4xl font-bold tracking-tight text-white">SoftCampus</h1>
        <p class="text-sm text-slate-400 mt-2 text-center px-6">"Todo el software educativo que necesitas, en un solo lugar."</p>
    </div>

    <!-- Login -->
    <div id="login-container" class="hidden min-h-screen flex items-center justify-center p-4">
        <div class="bg-slate-800 border border-slate-700 p-8 rounded-2xl shadow-2xl w-full max-w-md">
            <div class="text-center mb-6">
                <div class="bg-blue-600 inline-block p-3 rounded-xl text-white mb-3">
                    <svg xmlns="http://www.w3.org/2000/svg" width="32" height="32" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><rect width="20" height="14" x="2" y="3" rx="2"/><line x1="2" x2="22" y1="21" y2="21"/></svg>
                </div>
                <h1 class="text-2xl font-bold text-white">Acceso a SoftCampus</h1>
                <p class="text-xs text-slate-400 mt-1">Selecciona tu perfil de ingreso</p>
            </div>

            <div class="flex bg-slate-900 p-1 rounded-xl mb-6 border border-slate-700">
                <button onclick="cambiarTabLogin('estudiante')" id="tab-login-estudiante" class="flex-1 py-2 text-xs font-semibold rounded-lg bg-blue-600 text-white transition">Estudiante</button>
                <button onclick="cambiarTabLogin('admin')" id="tab-login-admin" class="flex-1 py-2 text-xs font-semibold rounded-lg text-slate-400 hover:text-white transition">Administrador</button>
            </div>

            <div id="form-login-estudiante" class="space-y-4">
                <p class="text-xs text-slate-400 text-center mb-2">Ingresa libremente como estudiante para consultar software y armar kits.</p>
                <button onclick="ingresarComoEstudiante()" class="w-full bg-blue-600 hover:bg-blue-500 text-white font-medium py-2.5 rounded-lg transition flex items-center justify-center gap-2">
                    <i data-lucide="graduation-cap" class="w-4 h-4"></i> Entrar como Estudiante
                </button>
            </div>

            <form id="form-login-admin" onsubmit="ingresarComoAdmin(event)" class="hidden space-y-4">
                <div>
                    <label class="block text-xs font-medium text-slate-400 mb-1">Correo Electrónico</label>
                    <input type="email" id="input-correo-admin" placeholder="admin@softcampus.edu" required class="w-full bg-slate-900 border border-slate-700 rounded-lg px-3 py-2 text-sm text-white focus:border-purple-500 outline-none">
                </div>
                <div>
                    <label class="block text-xs font-medium text-slate-400 mb-1">Contraseña de Administrador</label>
                    <input type="password" id="input-pass-admin" placeholder="••••••••" required class="w-full bg-slate-900 border border-slate-700 rounded-lg px-3 py-2 text-sm text-white focus:border-purple-500 outline-none">
                </div>
                <button type="submit" class="w-full bg-purple-600 hover:bg-purple-500 text-white font-medium py-2.5 rounded-lg transition flex items-center justify-center gap-2">
                    <i data-lucide="shield" class="w-4 h-4"></i> Acceder al Panel Admin
                </button>
            </form>
        </div>
    </div>

    <!-- App Principal -->
    <div id="app-container" class="hidden min-h-screen flex flex-col">
        <header class="bg-slate-800 border-b border-slate-700 sticky top-0 z-50">
            <div class="max-w-7xl mx-auto px-4 py-4 flex flex-col md:flex-row justify-between items-center gap-4">
                <div class="flex items-center gap-3">
                    <div class="bg-blue-600 p-2 rounded-lg text-white"><i data-lucide="laptop"></i></div>
                    <div>
                        <h1 class="text-xl font-bold tracking-tight text-white">SoftCampus</h1>
                        <p class="text-xs text-slate-400">"Todo el software educativo que necesitas, en un solo lugar."</p>
                    </div>
                </div>
                <div class="flex items-center gap-3 bg-slate-900 px-3 py-1.5 rounded-lg border border-slate-700">
                    <div class="text-right">
                        <p id="usuario-nombre" class="text-sm font-medium text-white leading-tight">Usuario</p>
                        <p id="usuario-rol" class="text-xs text-slate-400 leading-tight">Rol</p>
                    </div>
                    <button onclick="cerrarSesion()" class="text-xs bg-slate-800 text-red-400 border border-slate-700 px-3 py-1.5 rounded-lg flex items-center gap-1 hover:bg-slate-700 transition">
                        <i data-lucide="log-out" class="w-3.5 h-3.5"></i> Salir
                    </button>
                </div>
            </div>
        </header>

        <div class="max-w-7xl mx-auto px-4 py-6 flex-1 flex flex-col md:flex-row gap-6 w-full">
            <aside class="w-full md:w-64 flex-shrink-0">
                <!-- Menú Estudiante -->
                <nav id="nav-estudiante" class="hidden flex-col gap-1 bg-slate-800 p-3 rounded-xl border border-slate-700">
                    <div class="px-3 py-2 text-xs font-semibold text-slate-400 uppercase tracking-wider flex justify-between items-center">
                        <span>Menú Estudiante</span>
                        <span id="badge-notis-estudiante" class="hidden bg-blue-600 text-white text-[10px] px-1.5 py-0.5 rounded-full font-bold">Nueva</span>
                    </div>
                    <button onclick="navegar('catalogo')" class="nav-btn active flex items-center gap-3 px-3 py-2 rounded-lg text-sm font-medium hover:bg-slate-700 text-slate-200 transition" data-sec="catalogo"><i data-lucide="search" class="w-4 h-4"></i> Catálogo y Búsqueda</button>
                    <button onclick="navegar('recomendaciones')" class="nav-btn flex items-center gap-3 px-3 py-2 rounded-lg text-sm font-medium hover:bg-slate-700 text-slate-300 transition" data-sec="recomendaciones"><i data-lucide="graduation-cap" class="w-4 h-4"></i> Por Carrera</button>
                    <button onclick="navegar('favoritos')" class="nav-btn flex items-center gap-3 px-3 py-2 rounded-lg text-sm font-medium hover:bg-slate-700 text-slate-300 transition" data-sec="favoritos"><i data-lucide="star" class="w-4 h-4"></i> Mis Favoritos</button>
                    <button onclick="navegar('generator')" class="nav-btn flex items-center gap-3 px-3 py-2 rounded-lg text-sm font-medium hover:bg-slate-700 text-slate-300 transition" data-sec="generator"><i data-lucide="terminal" class="w-4 h-4"></i> Generador WinGet</button>
                    <button onclick="navegar('solicitar')" class="nav-btn flex items-center justify-between gap-3 px-3 py-2 rounded-lg text-sm font-medium hover:bg-slate-700 text-slate-300 transition" data-sec="solicitar">
                        <span class="flex items-center gap-3"><i data-lucide="send" class="w-4 h-4"></i> Solicitar Software</span>
                        <span id="badge-punto-estudiante" class="hidden w-2 h-2 bg-blue-500 rounded-full"></span>
                    </button>
                </nav>

                <!-- Menú Admin -->
                <nav id="nav-admin" class="hidden flex-col gap-1 bg-slate-800 p-3 rounded-xl border border-slate-700">
                    <div class="px-3 py-2 text-xs font-semibold text-purple-400 uppercase tracking-wider">Menú Administrador</div>
                    <button onclick="navegar('registrar')" class="nav-btn flex items-center gap-3 px-3 py-2 rounded-lg text-sm font-medium hover:bg-slate-700 text-slate-300 transition" data-sec="registrar"><i data-lucide="plus-circle" class="w-4 h-4 text-blue-400"></i> Registrar Software</button>
                    <button onclick="navegar('crear-kit')" class="nav-btn flex items-center gap-3 px-3 py-2 rounded-lg text-sm font-medium hover:bg-slate-700 text-slate-300 transition" data-sec="crear-kit"><i data-lucide="folder-plus" class="w-4 h-4 text-green-400"></i> Gestionar Kits</button>
                    <button onclick="navegar('verificador')" class="nav-btn flex items-center gap-3 px-3 py-2 rounded-lg text-sm font-medium hover:bg-slate-700 text-slate-300 transition" data-sec="verificador"><i data-lucide="check-circle" class="w-4 h-4 text-purple-400"></i> Verificador y Edición</button>
                    <button onclick="navegar('solicitudes')" class="nav-btn flex items-center justify-between gap-3 px-3 py-2 rounded-lg text-sm font-medium hover:bg-slate-700 text-slate-300 transition" data-sec="solicitudes">
                        <span class="flex items-center gap-3"><i data-lucide="inbox" class="w-4 h-4 text-yellow-400"></i> Solicitudes</span>
                        <span id="badge-solicitudes" class="hidden bg-red-600 text-white text-[10px] font-bold px-1.5 py-0.5 rounded-full">0</span>
                    </button>
                </nav>
            </aside>
            <main class="flex-1 bg-slate-800 p-6 rounded-xl border border-slate-700">
                <section id="sec-catalogo" class="sec-content">
                    <div class="flex justify-between items-center mb-4">
                        <h2 class="text-2xl font-bold flex items-center gap-2"><i data-lucide="search" class="text-blue-500"></i> Catálogo General</h2>
                        <span id="total-programas-badge" class="bg-blue-900/60 text-blue-300 text-xs px-3 py-1 rounded-full border border-blue-700">0 Programas</span>
                    </div>
                    <div class="grid grid-cols-1 md:grid-cols-3 gap-4 mb-6 bg-slate-900 p-4 rounded-lg border border-slate-700">
                        <div><label class="block text-xs font-medium text-slate-400 mb-1">Nombre</label><input type="text" id="filter-nombre" oninput="filtrarCatalog()" placeholder="Ej. Visual Studio" class="w-full bg-slate-800 border border-slate-700 rounded-lg px-3 py-2 text-sm text-white"></div>
                        <div>
                            <label class="block text-xs font-medium text-slate-400 mb-1">Categoría</label>
                            <select id="filter-categoria" onchange="filtrarCatalog()" class="w-full bg-slate-800 border border-slate-700 rounded-lg px-3 py-2 text-sm text-white">
                                <option value="">Todas</option><option value="Programación">Programación</option><option value="Diseño">Diseño</option><option value="Electrónica">Electrónica</option><option value="General">General</option>
                            </select>
                        </div>
                        <div>
                            <label class="block text-xs font-medium text-slate-400 mb-1">Licencia</label>
                            <select id="filter-licencia" onchange="filtrarCatalog()" class="w-full bg-slate-800 border border-slate-700 rounded-lg px-3 py-2 text-sm text-white">
                                <option value="">Todas</option><option value="Libre">Libre</option><option value="Gratuito">Gratuito</option><option value="Comercial">Comercial</option>
                            </select>
                        </div>
                    </div>
                    <div id="contenedor-software" class="grid grid-cols-1 gap-4"></div>
                </section>
                <section id="sec-recomendaciones" class="sec-content hidden">
                    <h2 class="text-2xl font-bold mb-4 flex items-center gap-2"><i data-lucide="graduation-cap" class="text-blue-500"></i> Recomendaciones por Carrera</h2>
                    <div class="mb-6">
                        <label class="block text-sm font-medium text-slate-300 mb-2">Selecciona la carrera:</label>
                        <select id="select-carrera" onchange="renderRecomendaciones()" class="bg-slate-900 border border-slate-700 rounded-lg px-4 py-2 text-white w-full md:w-64">
                            <option value="Informática">Informática</option><option value="Diseño">Diseño</option><option value="Electrónica">Electrónica</option>
                        </select>
                    </div>
                    <div id="contenedor-recomendaciones" class="grid grid-cols-1 md:grid-cols-2 gap-4"></div>
                </section>
                <section id="sec-favoritos" class="sec-content hidden">
                    <h2 class="text-2xl font-bold mb-4 flex items-center gap-2"><i data-lucide="star" class="text-yellow-500"></i> Mis Programas Favoritos</h2>
                    <div id="contenedor-favoritos" class="grid grid-cols-1 gap-4"></div>
                </section>
                <section id="sec-generator" class="sec-content hidden">
                    <h2 class="text-2xl font-bold mb-2 flex items-center gap-2"><i data-lucide="terminal" class="text-green-500"></i> Generador de Kit (WinGet)</h2>
                    <p class="text-slate-400 text-sm mb-4">Elige un kit ya armado o selecciona programas para automatizar instalaciones.</p>
                    <div class="mb-6">
                        <h3 class="font-semibold mb-3 text-slate-200 flex items-center gap-2"><i data-lucide="package" class="w-4 h-4 text-blue-400"></i> Kits predefinidos y personalizados</h3>
                        <div id="contenedor-kits" class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-3"></div>
                    </div>
                    <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
                        <div class="bg-slate-900 p-4 rounded-lg border border-slate-700">
                            <div class="flex justify-between items-center mb-3">
                                <h3 class="font-semibold text-slate-200">Personaliza tu selección:</h3>
                                <button onclick="limpiarSeleccionKit()" class="text-xs text-slate-400 hover:text-white">Limpiar</button>
                            </div>
                            <div id="contenedor-checklist-winget" class="space-y-2 max-h-60 overflow-y-auto pr-2"></div>
                        </div>
                        <div class="flex flex-col">
                            <h3 class="font-semibold mb-2 text-slate-200">Vista Previa:</h3>
                            <pre id="codigo-bat" class="bg-slate-950 p-4 rounded-lg border border-slate-700 text-green-400 font-mono text-xs overflow-x-auto flex-1 mb-4">@echo off&#10;echo Selecciona un kit...</pre>
                            <button onclick="descargarScriptBat()" class="bg-green-700 hover:bg-green-600 text-white py-2 px-4 rounded-lg flex items-center justify-center gap-2"><i data-lucide="download" class="w-4 h-4"></i> Descargar instalar.bat</button>
                        </div>
                    </div>
                </section>
                <section id="sec-registrar" class="sec-content hidden">
                    <h2 class="text-2xl font-bold mb-4 flex items-center gap-2 text-blue-400"><i data-lucide="plus-circle"></i> Añadir Nuevo Software</h2>
                    <form id="form-nuevo-software" onsubmit="guardarNuevoSoftware(event)" class="grid grid-cols-1 md:grid-cols-2 gap-4 bg-slate-900 p-5 rounded-xl border border-slate-700">
                        <div><label class="block text-xs font-medium text-slate-400 mb-1">Nombre *</label><input type="text" id="reg-nombre" required class="w-full bg-slate-800 border border-slate-700 rounded-lg px-3 py-2 text-sm text-white"></div>
                        <div><label class="block text-xs font-medium text-slate-400 mb-1">Categoría *</label><select id="reg-categoria" required class="w-full bg-slate-800 border border-slate-700 rounded-lg px-3 py-2 text-sm text-white"><option value="Programación">Programación</option><option value="Diseño">Diseño</option><option value="Electrónica">Electrónica</option><option value="General">General</option></select></div>
                        <div><label class="block text-xs font-medium text-slate-400 mb-1">Licencia *</label><select id="reg-licencia" required class="w-full bg-slate-800 border border-slate-700 rounded-lg px-3 py-2 text-sm text-white"><option value="Libre">Libre</option><option value="Gratuito">Gratuito</option><option value="Comercial">Comercial</option></select></div>
                        <div><label class="block text-xs font-medium text-slate-400 mb-1">Sistemas Operativos *</label><input type="text" id="reg-so" value="Windows, Linux, Mac" required class="w-full bg-slate-800 border border-slate-700 rounded-lg px-3 py-2 text-sm text-white"></div>
                        <div><label class="block text-xs font-medium text-slate-400 mb-1">Versión *</label><input type="text" id="reg-version" value="1.0" required class="w-full bg-slate-800 border border-slate-700 rounded-lg px-3 py-2 text-sm text-white"></div>
                        <div><label class="block text-xs font-medium text-slate-400 mb-1">Carrera Asociada *</label><select id="reg-carrera" required class="w-full bg-slate-800 border border-slate-700 rounded-lg px-3 py-2 text-sm text-white"><option value="Informática">Informática</option><option value="Diseño">Diseño</option><option value="Electrónica">Electrónica</option></select></div>
                        <div><label class="block text-xs font-medium text-slate-400 mb-1">Página Web (URL) *</label><input type="url" id="reg-url" required class="w-full bg-slate-800 border border-slate-700 rounded-lg px-3 py-2 text-sm text-white"></div>
                        <div><label class="block text-xs font-medium text-slate-400 mb-1">WinGet ID</label><input type="text" id="reg-winget" class="w-full bg-slate-800 border border-slate-700 rounded-lg px-3 py-2 text-sm text-white"></div>
                        <div class="md:col-span-2"><label class="block text-xs font-medium text-slate-400 mb-1">Descripción *</label><textarea id="reg-descripcion" required class="w-full bg-slate-800 border border-slate-700 rounded-lg px-3 py-2 text-sm text-white h-20"></textarea></div>
                        <div class="md:col-span-2 flex justify-between pt-2">
                            <button type="submit" class="bg-blue-600 hover:bg-blue-500 text-white font-medium py-2.5 px-6 rounded-lg flex items-center gap-2"><i data-lucide="save" class="w-4 h-4"></i> Guardar Software</button>
                        </div>
                    </form>
                </section>

                <!-- Sección Crear Kit (Admin) -->
                <section id="sec-crear-kit" class="sec-content hidden">
                    <h2 class="text-2xl font-bold mb-4 flex items-center gap-2 text-green-400"><i data-lucide="folder-plus"></i> Crear Kit de Software</h2>
                    <form id="form-nuevo-kit" onsubmit="guardarNuevoKit(event)" class="bg-slate-900 p-5 rounded-xl border border-slate-700 space-y-4 mb-6">
                        <div class="grid grid-cols-1 md:grid-cols-2 gap-4">
                            <div>
                                <label class="block text-xs font-medium text-slate-400 mb-1">Nombre del Kit *</label>
                                <input type="text" id="kit-nombre" required placeholder="Ej. Kit Avanzado de Diseño" class="w-full bg-slate-800 border border-slate-700 rounded-lg px-3 py-2 text-sm text-white">
                            </div>
                            <div>
                                <label class="block text-xs font-medium text-slate-400 mb-1">Carrera Destinada *</label>
                                <select id="kit-carrera" required class="w-full bg-slate-800 border border-slate-700 rounded-lg px-3 py-2 text-sm text-white">
                                    <option value="Informática">Informática</option>
                                    <option value="Diseño">Diseño</option>
                                    <option value="Electrónica">Electrónica</option>
                                    <option value="General">General</option>
                                </select>
                            </div>
                        </div>
                        <div>
                            <label class="block text-xs font-medium text-slate-400 mb-2">Selecciona los programas que incluirá el Kit:</label>
                            <div id="contenedor-check-kit-admin" class="grid grid-cols-1 sm:grid-cols-2 md:grid-cols-3 gap-2 max-h-48 overflow-y-auto p-2 bg-slate-800 rounded-lg border border-slate-700"></div>
                        </div>
                        <button type="submit" class="bg-green-600 hover:bg-green-500 text-white font-medium py-2.5 px-6 rounded-lg flex items-center gap-2">
                            <i data-lucide="save" class="w-4 h-4"></i> Guardar Kit
                        </button>
                    </form>

                    <h3 class="text-lg font-bold mb-3 text-slate-200">Kits Personalizados Creados</h3>
                    <div id="contenedor-lista-kits-admin" class="space-y-2"></div>
                </section>

                <section id="sec-verificador" class="sec-content hidden">
                    <h2 class="text-2xl font-bold mb-4 flex items-center gap-2"><i data-lucide="check-circle" class="text-purple-500"></i> Verificador y Gestión</h2>
                    <div id="contenedor-verificador" class="space-y-3"></div>
                </section>
                <section id="sec-solicitar" class="sec-content hidden">
                    <h2 class="text-2xl font-bold mb-4 flex items-center gap-2 text-blue-400"><i data-lucide="send"></i> Solicitar Software</h2>
                    <form id="form-solicitud" onsubmit="enviarSolicitud(event)" class="grid grid-cols-1 md:grid-cols-2 gap-4 bg-slate-900 p-5 rounded-xl border border-slate-700">
                        <div><label class="block text-xs font-medium text-slate-400 mb-1">Nombre *</label><input type="text" id="sol-nombre" required class="w-full bg-slate-800 border border-slate-700 rounded-lg px-3 py-2 text-sm text-white"></div>
                        <div><label class="block text-xs font-medium text-slate-400 mb-1">Carrera *</label><select id="sol-carrera" required class="w-full bg-slate-800 border border-slate-700 rounded-lg px-3 py-2 text-sm text-white"><option value="Informática">Informática</option><option value="Diseño">Diseño</option><option value="Electrónica">Electrónica</option><option value="Otra">Otra</option></select></div>
                        <div class="md:col-span-2"><label class="block text-xs font-medium text-slate-400 mb-1">¿Para qué lo necesitas? *</label><textarea id="sol-comentario" required class="w-full bg-slate-800 border border-slate-700 rounded-lg px-3 py-2 text-sm text-white h-24"></textarea></div>
                        <div class="md:col-span-2"><button type="submit" class="bg-blue-600 hover:bg-blue-500 text-white font-medium py-2.5 px-6 rounded-lg flex items-center gap-2"><i data-lucide="send" class="w-4 h-4"></i> Enviar Solicitud</button></div>
                    </form>
                    <div class="mt-6">
                        <h3 class="text-sm font-semibold text-slate-400 mb-2">Mis solicitudes enviadas</h3>
                        <div id="contenedor-mis-solicitudes" class="space-y-2"></div>
                    </div>
                </section>
                <section id="sec-solicitudes" class="sec-content hidden">
                    <h2 class="text-2xl font-bold mb-4 flex items-center gap-2 text-purple-400"><i data-lucide="inbox"></i> Solicitudes de Estudiantes</h2>
                    <div id="contenedor-solicitudes" class="space-y-3"></div>
                </section>
            </main>
        </div>
    </div>

    <!-- Modal Editar Software -->
    <div id="modal-editar" class="fixed inset-0 bg-black/70 backdrop-blur-sm z-50 flex items-center justify-center hidden p-4">
        <div class="bg-slate-800 border border-slate-700 rounded-xl p-6 w-full max-w-2xl">
            <div class="flex justify-between items-center mb-4 pb-2 border-b border-slate-700">
                <h3 class="text-xl font-bold text-white flex items-center gap-2"><i data-lucide="edit" class="text-blue-400"></i> Editar Programa</h3>
                <button onclick="cerrarModalEditar()" class="text-slate-400 hover:text-white p-1"><i data-lucide="x" class="w-5 h-5"></i></button>
            </div>
            <form id="form-editar-software" onsubmit="guardarEdicionSoftware(event)" class="grid grid-cols-1 md:grid-cols-2 gap-4">
                <input type="hidden" id="edit-id">
                <div><label class="block text-xs font-medium text-slate-400 mb-1">Nombre *</label><input type="text" id="edit-nombre" required class="w-full bg-slate-900 border border-slate-700 rounded-lg px-3 py-2 text-sm text-white"></div>
                <div><label class="block text-xs font-medium text-slate-400 mb-1">Categoría *</label><select id="edit-categoria" required class="w-full bg-slate-900 border border-slate-700 rounded-lg px-3 py-2 text-sm text-white"><option value="Programación">Programación</option><option value="Diseño">Diseño</option><option value="Electrónica">Electrónica</option><option value="General">General</option></select></div>
                <div><label class="block text-xs font-medium text-slate-400 mb-1">Licencia *</label><select id="edit-licencia" required class="w-full bg-slate-900 border border-slate-700 rounded-lg px-3 py-2 text-sm text-white"><option value="Libre">Libre</option><option value="Gratuito">Gratuito</option><option value="Comercial">Comercial</option></select></div>
                <div><label class="block text-xs font-medium text-slate-400 mb-1">Sistemas *</label><input type="text" id="edit-so" required class="w-full bg-slate-900 border border-slate-700 rounded-lg px-3 py-2 text-sm text-white"></div>
                <div><label class="block text-xs font-medium text-slate-400 mb-1">Versión *</label><input type="text" id="edit-version" required class="w-full bg-slate-900 border border-slate-700 rounded-lg px-3 py-2 text-sm text-white"></div>
                <div><label class="block text-xs font-medium text-slate-400 mb-1">Carrera *</label><select id="edit-carrera" required class="w-full bg-slate-900 border border-slate-700 rounded-lg px-3 py-2 text-sm text-white"><option value="Informática">Informática</option><option value="Diseño">Diseño</option><option value="Electrónica">Electrónica</option></select></div>
                <div><label class="block text-xs font-medium text-slate-400 mb-1">URL Oficial *</label><input type="url" id="edit-url" required class="w-full bg-slate-900 border border-slate-700 rounded-lg px-3 py-2 text-sm text-white"></div>
                <div><label class="block text-xs font-medium text-slate-400 mb-1">WinGet ID</label><input type="text" id="edit-winget" class="w-full bg-slate-900 border border-slate-700 rounded-lg px-3 py-2 text-sm text-white"></div>
                <div class="md:col-span-2"><label class="block text-xs font-medium text-slate-400 mb-1">Descripción *</label><textarea id="edit-descripcion" required class="w-full bg-slate-900 border border-slate-700 rounded-lg px-3 py-2 text-sm text-white h-20"></textarea></div>
                <div class="md:col-span-2 flex justify-end gap-3 pt-2 border-t border-slate-700">
                    <button type="button" onclick="cerrarModalEditar()" class="px-4 py-2 text-sm text-slate-300 hover:text-white">Cancelar</button>
                    <button type="submit" class="bg-blue-600 hover:bg-blue-500 text-white text-sm font-medium px-5 py-2 rounded-lg flex items-center gap-2"><i data-lucide="check" class="w-4 h-4"></i> Guardar Cambios</button>
                </div>
            </form>
        </div>
    </div>

    <!-- Toast -->
    <div id="toast-container" class="fixed bottom-5 right-5 z-50 flex flex-col gap-3"></div>

    <!-- Scripts -->
    <script>
        let softwareBD = [];
        let solicitudes = [];
        let kitsBD = [];
        let favoritos = JSON.parse(localStorage.getItem('softcampus_favs')) || [1, 2];
        let rolActual = '';
        const correoAdminValido = 'admin@softcampus.edu';
        const passwordAdminValido = 'admin123';

        const datosIniciales = [
            { id: 1, nombre: "Visual Studio Code", categoria: "Programación", carrera: "Informática", so: "Windows, Linux, Mac", licencia: "Gratuito", version: "1.108", url: "https://code.visualstudio.com", winget: "Microsoft.VisualStudioCode", desc: "Editor de código fuente ligero." }
        ];

        async function cargarDatosDesdeAPI() {
            try {
                const resSoft = await fetch('/api/software');
                if (resSoft.ok) softwareBD = await resSoft.json();
                else softwareBD = datosIniciales;

                const resSol = await fetch('/api/solicitudes');
                if (resSol.ok) solicitudes = await resSol.json();

                const resKits = await fetch('/api/kits');
                if (resKits.ok) kitsBD = await resKits.json();
            } catch (error) {
                softwareBD = datosIniciales;
            }
            renderCatalogo(softwareBD);
            actualizarBadgeSolicitudes();
            verificarNotificacionesEstudiante();
        }

        function cambiarTabLogin(tipo) {
            const btnEst = document.getElementById('tab-login-estudiante');
            const btnAdmin = document.getElementById('tab-login-admin');
            const formEst = document.getElementById('form-login-estudiante');
            const formAdmin = document.getElementById('form-login-admin');

            if (tipo === 'estudiante') {
                btnEst.className = "flex-1 py-2 text-xs font-semibold rounded-lg bg-blue-600 text-white transition";
                btnAdmin.className = "flex-1 py-2 text-xs font-semibold rounded-lg text-slate-400 hover:text-white transition";
                formEst.classList.remove('hidden');
                formAdmin.classList.add('hidden');
            } else {
                btnAdmin.className = "flex-1 py-2 text-xs font-semibold rounded-lg bg-purple-600 text-white transition";
                btnEst.className = "flex-1 py-2 text-xs font-semibold rounded-lg text-slate-400 hover:text-white transition";
                formAdmin.classList.remove('hidden');
                formEst.classList.add('hidden');
            }
        }

        function ingresarComoEstudiante() {
            rolActual = 'estudiante';
            localStorage.setItem('softcampus_rol', 'estudiante');
            document.getElementById('usuario-nombre').textContent = 'Estudiante';
            document.getElementById('usuario-rol').textContent = 'Modo Consulta & Kits';
            iniciarSesionExitosa('catalogo');
        }

        function ingresarComoAdmin(e) {
            e.preventDefault();
            const correo = document.getElementById('input-correo-admin').value.trim();
            const pass = document.getElementById('input-pass-admin').value;

            if (correo === correoAdminValido && pass === passwordAdminValido) {
                rolActual = 'admin';
                localStorage.setItem('softcampus_rol', 'admin');
                document.getElementById('usuario-nombre').textContent = 'Administrador';
                document.getElementById('usuario-rol').textContent = correo;
                document.getElementById('input-correo-admin').value = '';
                document.getElementById('input-pass-admin').value = '';
                iniciarSesionExitosa('registrar');
            } else {
                mostrarToast("Correo o contraseña de administrador incorrectos", "error");
            }
        }

        function iniciarSesionExitosa(seccionGuardada = null) {
            document.getElementById('login-container').classList.add('hidden');
            document.getElementById('app-container').classList.remove('hidden');

            const navEst = document.getElementById('nav-estudiante');
            const navAdmin = document.getElementById('nav-admin');

            if (rolActual === 'admin') {
                navAdmin.classList.remove('hidden');
                navEst.classList.add('hidden');
                navegar(seccionGuardada || 'registrar');

                const pendientes = solicitudes.filter(s => s.estado === 'pendiente').length;
                if (pendientes > 0) mostrarToast(`Hay ${pendientes} solicitud(es) pendiente(s)`, 'error');
            } else {
                navEst.classList.remove('hidden');
                navAdmin.classList.add('hidden');
                navegar(seccionGuardada || 'catalogo');
                verificarNotificacionesEstudiante();
            }
            lucide.createIcons();
        }

        function cerrarSesion() {
            rolActual = '';
            localStorage.removeItem('softcampus_rol');
            localStorage.removeItem('softcampus_seccion');
            document.getElementById('app-container').classList.add('hidden');
            document.getElementById('login-container').classList.remove('hidden');
            cambiarTabLogin('estudiante');
            mostrarToast("Sesión cerrada", 'exito');
        }

        async function guardarNuevoSoftware(e) {
            e.preventDefault();
            if (rolActual !== 'admin') return;
            const nuevo = {
                nombre: document.getElementById('reg-nombre').value,
                categoria: document.getElementById('reg-categoria').value,
                licencia: document.getElementById('reg-licencia').value,
                so: document.getElementById('reg-so').value,
                version: document.getElementById('reg-version').value,
                carrera: document.getElementById('reg-carrera').value,
                url: document.getElementById('reg-url').value,
                winget: document.getElementById('reg-winget').value.trim(),
                desc: document.getElementById('reg-descripcion').value
            };

            try {
                const res = await fetch('/api/software', {
                    method: 'POST',
                    headers: { 'Content-Type': 'application/json' },
                    body: JSON.stringify(nuevo)
                });
                if (res.ok) {
                    mostrarToast(`"${nuevo.nombre}" añadido con éxito`, 'exito');
                    document.getElementById('form-nuevo-software').reset();
                    await cargarDatosDesdeAPI();
                    navegar('verificador');
                } else {
                    mostrarToast("Error al guardar en el servidor", "error");
                }
            } catch (error) { mostrarToast("Error de conexión", "error"); }
        }

        async function guardarNuevoKit(e) {
            e.preventDefault();
            if (rolActual !== 'admin') return;

            const checks = document.querySelectorAll('.kit-admin-checkbox:checked');
            const wingetIds = Array.from(checks).map(chk => chk.value);

            if (wingetIds.length === 0) {
                mostrarToast("Selecciona al menos un programa con WinGet para el kit", "error");
                return;
            }

            const nuevoKit = {
                nombre: document.getElementById('kit-nombre').value.trim(),
                carrera: document.getElementById('kit-carrera').value,
                programas: JSON.stringify(wingetIds)
            };

            try {
                const res = await fetch('/api/kits', {
                    method: 'POST',
                    headers: { 'Content-Type': 'application/json' },
                    body: JSON.stringify(nuevoKit)
                });
                if (res.ok) {
                    mostrarToast("Kit creado con éxito", "exito");
                    document.getElementById('form-nuevo-kit').reset();
                    await cargarDatosDesdeAPI();
                    renderAdminKits();
                } else {
                    mostrarToast("Error al guardar el kit", "error");
                }
            } catch (err) {
                mostrarToast("Error de conexión", "error");
            }
        }

        async function eliminarKitAdmin(id) {
            if (rolActual !== 'admin') return;
            if (confirm("¿Deseas eliminar este kit?")) {
                try {
                    const res = await fetch(`/api/kits/${id}`, { method: 'DELETE' });
                    if (res.ok) {
                        mostrarToast("Kit eliminado", "exito");
                        await cargarDatosDesdeAPI();
                        renderAdminKits();
                    }
                } catch (e) { mostrarToast("Error al eliminar", "error"); }
            }
        }

        function renderAdminKits() {
            const contCheck = document.getElementById('contenedor-check-kit-admin');
            const disponibles = softwareBD.filter(s => s.winget && s.winget.trim() !== '');

            contCheck.innerHTML = disponibles.length === 0 ? '<p class="text-xs text-slate-400">No hay software con WinGet ID registrado.</p>' :
                disponibles.map(item => `
                    <label class="flex items-center gap-2 text-xs text-slate-200 cursor-pointer bg-slate-900 p-2 rounded border border-slate-700">
                        <input type="checkbox" value="${item.winget}" class="kit-admin-checkbox"> ${item.nombre}
                    </label>
                `).join('');

            const contLista = document.getElementById('contenedor-lista-kits-admin');
            contLista.innerHTML = kitsBD.length === 0 ? '<p class="text-sm text-slate-500">No hay kits personalizados creados.</p>' :
                kitsBD.map(k => `
                    <div class="bg-slate-900 border border-slate-700 p-3 rounded-lg flex justify-between items-center">
                        <div>
                            <h4 class="font-bold text-white text-sm">${k.nombre} <span class="text-xs font-normal text-blue-400">(${k.carrera})</span></h4>
                            <p class="text-xs text-slate-400">Programas ID: ${k.programas}</p>
                        </div>
                        <button onclick="eliminarKitAdmin(${k.id})" class="bg-red-950/60 text-red-300 text-xs px-3 py-1.5 rounded-lg border border-red-900">Eliminar</button>
                    </div>
                `).join('');
            lucide.createIcons();
        }

        async function guardarEdicionSoftware(e) {
            e.preventDefault();
            if (rolActual !== 'admin') return;
            const id = document.getElementById('edit-id').value;
            const editado = {
                nombre: document.getElementById('edit-nombre').value,
                categoria: document.getElementById('edit-categoria').value,
                licencia: document.getElementById('edit-licencia').value,
                so: document.getElementById('edit-so').value,
                version: document.getElementById('edit-version').value,
                carrera: document.getElementById('edit-carrera').value,
                url: document.getElementById('edit-url').value,
                winget: document.getElementById('edit-winget').value.trim(),
                desc: document.getElementById('edit-descripcion').value
            };

            try {
                const res = await fetch(`/api/software/${id}`, {
                    method: 'PUT',
                    headers: { 'Content-Type': 'application/json' },
                    body: JSON.stringify(editado)
                });
                if (res.ok) {
                    cerrarModalEditar();
                    mostrarToast("Programa actualizado", 'exito');
                    await cargarDatosDesdeAPI();
                    renderVerificador();
                }
            } catch (error) { mostrarToast("Error al actualizar", "error"); }
        }

        async function eliminarSoftware(id) {
            if (rolActual !== 'admin') return;
            if (confirm("¿Deseas eliminar este programa del catálogo?")) {
                try {
                    const res = await fetch(`/api/software/${id}`, { method: 'DELETE' });
                    if (res.ok) {
                        mostrarToast("Programa eliminado", 'exito');
                        await cargarDatosDesdeAPI();
                        renderVerificador();
                    }
                } catch (error) { mostrarToast("Error al eliminar", "error"); }
            }
        }

        async function enviarSolicitud(e) {
            e.preventDefault();
            const nueva = {
                nombre: document.getElementById('sol-nombre').value,
                carrera: document.getElementById('sol-carrera').value,
                comentario: document.getElementById('sol-comentario').value,
                solicitadoPor: 'Estudiante',
                fecha: new Date().toLocaleDateString('es-ES'),
                estado: 'pendiente'
            };

            try {
                const res = await fetch('/api/solicitudes', {
                    method: 'POST',
                    headers: { 'Content-Type': 'application/json' },
                    body: JSON.stringify(nueva)
                });
                if (res.ok) {
                    mostrarToast("Solicitud enviada al administrador", 'exito');
                    document.getElementById('form-solicitud').reset();
                    await cargarDatosDesdeAPI();
                    renderMisSolicitudes();
                }
            } catch (error) { mostrarToast("Error al enviar", "error"); }
        }

        async function atenderSolicitud(id) {
            if (rolActual !== 'admin') return;
            try {
                const res = await fetch(`/api/solicitudes/${id}`, {
                    method: 'PUT',
                    headers: { 'Content-Type': 'application/json' },
                    body: JSON.stringify({ estado: 'atendida', notificadoLeido: false })
                });
                if (res.ok) {
                    mostrarToast('Solicitud marcada como atendida', 'exito');
                    await cargarDatosDesdeAPI();
                    renderSolicitudesAdmin();
                }
            } catch (error) { mostrarToast("Error al actualizar", "error"); }
        }

        async function eliminarSolicitud(id) {
            if (rolActual !== 'admin') return;
            try {
                const res = await fetch(`/api/solicitudes/${id}`, { method: 'DELETE' });
                if (res.ok) {
                    mostrarToast('Solicitud eliminada', 'exito');
                    await cargarDatosDesdeAPI();
                    renderSolicitudesAdmin();
                }
            } catch (error) { mostrarToast("Error al eliminar", "error"); }
        }

        function verificarNotificacionesEstudiante() {
            if (rolActual !== 'estudiante') return;
            const atendidasNuevas = solicitudes.filter(s => s.estado === 'atendida');
            const badgeTexto = document.getElementById('badge-notis-estudiante');
            const badgePunto = document.getElementById('badge-punto-estudiante');

            if (atendidasNuevas.length > 0) {
                if(badgeTexto) badgeTexto.classList.remove('hidden');
                if(badgePunto) badgePunto.classList.remove('hidden');
            } else {
                if(badgeTexto) badgeTexto.classList.add('hidden');
                if(badgePunto) badgePunto.classList.add('hidden');
            }
        }

        function marcarNotificacionLeida(id) {}

        function toggleFavorito(id) {
            if (favoritos.includes(id)) favoritos = favoritos.filter(fId => fId !== id);
            else favoritos.push(id);
            localStorage.setItem('softcampus_favs', JSON.stringify(favoritos));
            renderCatalogo(softwareBD);
        }

        function mostrarToast(mensaje, tipo = 'exito') {
            const contenedor = document.getElementById('toast-container');
            const toast = document.createElement('div');
            toast.className = `${tipo === 'exito' ? 'bg-green-600' : 'bg-red-600'} text-white px-6 py-3 rounded-lg shadow-lg transform transition-all duration-300 translate-y-10 opacity-0 flex items-center font-semibold text-sm`;
            toast.innerText = mensaje;
            contenedor.appendChild(toast);
            setTimeout(() => toast.classList.remove('translate-y-10', 'opacity-0'), 10);
            setTimeout(() => {
                toast.classList.add('opacity-0', 'translate-y-10');
                setTimeout(() => toast.remove(), 300);
            }, 3000);
        }

        function navegar(secId, guardarEnStorage = true) {
            document.querySelectorAll('.sec-content').forEach(s => s.classList.add('hidden'));
            const seccionActiva = document.getElementById(`sec-${secId}`);
            if (seccionActiva) seccionActiva.classList.remove('hidden');

            document.querySelectorAll('.nav-btn').forEach(b => {
                if (b.dataset.sec === secId) b.classList.add('bg-slate-700', 'text-white');
                else b.classList.remove('bg-slate-700', 'text-white');
            });

            if (guardarEnStorage && rolActual) {
                localStorage.setItem('softcampus_seccion', secId);
            }

            if (secId === 'catalogo') renderCatalogo(softwareBD);
            if (secId === 'recomendaciones') renderRecomendaciones();
            if (secId === 'favoritos') renderFavoritos();
            if (secId === 'generator') renderGenerator();
            if (secId === 'crear-kit') renderAdminKits();
            if (secId === 'verificador') renderVerificador();
            if (secId === 'solicitar') renderMisSolicitudes();
            if (secId === 'solicitudes') renderSolicitudesAdmin();
        }

        function renderCatalogo(lista) {
            const cont = document.getElementById('contenedor-software');
            document.getElementById('total-programas-badge').textContent = `${softwareBD.length} Programas`;
            cont.innerHTML = '';
            if (lista.length === 0) {
                cont.innerHTML = `<div class="text-center py-10"><p class="text-slate-400">No hay programas disponibles con estos filtros.</p></div>`; return;
            }
            lista.forEach(item => {
                const esFav = favoritos.includes(item.id);
                cont.innerHTML += `
                    <div class="bg-slate-900 border border-slate-700 rounded-lg p-4 flex flex-col md:flex-row justify-between items-start md:items-center gap-4">
                        <div class="flex-1">
                            <div class="flex flex-wrap items-center gap-2">
                                <h3 class="font-bold text-lg text-white">${item.nombre}</h3>
                                <span class="bg-blue-900/60 text-blue-300 text-xs px-2 py-0.5 rounded border border-blue-700/50">${item.categoria}</span>
                            </div>
                            <p class="text-slate-300 text-sm mt-1">${item.desc}</p>
                            <div class="flex flex-wrap gap-4 text-xs text-slate-400 mt-2">
                                <span>💻 SO: ${item.so}</span> <span>📄 Lic: ${item.licencia}</span> <span>🎓 Carrera: ${item.carrera}</span>
                                ${item.winget ? `<span class="text-green-400 font-mono">⚡ WinGet: ${item.winget}</span>` : '<span class="text-yellow-500">⚠️ Sin WinGet</span>'}
                            </div>
                        </div>
                        <div class="flex flex-wrap items-center gap-2">
                            <button onclick="toggleFavorito(${item.id})" class="p-2 rounded-lg border border-slate-700 ${esFav ? 'bg-yellow-500/10 text-yellow-500 border-yellow-500/30' : 'text-slate-400 hover:text-white'}"><i data-lucide="star" class="w-4 h-4 ${esFav ? 'fill-yellow-500' : ''}"></i></button>
                            <a href="${item.url}" target="_blank" class="bg-slate-800 text-blue-400 border border-slate-700 text-xs px-3 py-2 rounded-lg flex items-center gap-1">Web <i data-lucide="external-link" class="w-3 h-3"></i></a>
                            <button onclick="instalarPrograma(${item.id})" class="bg-green-700 text-white text-xs px-3 py-2 rounded-lg flex items-center gap-1"><i data-lucide="download" class="w-3.5 h-3.5"></i> Instalar</button>
                        </div>
                    </div>`;
            });
            lucide.createIcons();
        }

        function filtrarCatalog() {
            const nom = document.getElementById('filter-nombre').value.toLowerCase();
            const cat = document.getElementById('filter-categoria').value;
            const lic = document.getElementById('filter-licencia').value;
            const res = softwareBD.filter(s => (s.nombre.toLowerCase().includes(nom) || s.desc.toLowerCase().includes(nom)) && (cat === '' || s.categoria === cat) && (lic === '' || s.licencia === lic));
            renderCatalogo(res);
        }

        function instalarPrograma(id) {
            const item = softwareBD.find(s => s.id === id);
            if (!item) return;
            if (item.winget && item.winget.length > 0) {
                let script = `@echo off\necho Instalando ${item.nombre} via WinGet...\nwinget install --id ${item.winget} -e --accept-package-agreements\npause\n`;
                const blob = new Blob([script], { type: 'text/plain' });
                const enlace = document.createElement('a');
                enlace.href = URL.createObjectURL(blob);
                enlace.download = `instalar_${item.nombre.replace(/[^a-zA-Z0-9]+/g, '_')}.bat`;
                enlace.click();
            } else {
                if (confirm(`Sin WinGet. ¿Ir a la web oficial?`)) window.open(item.url, '_blank');
            }
        }

        function abrirModalEditar(id) {
            if (rolActual !== 'admin') return;
            const item = softwareBD.find(s => s.id === id);
            if (!item) return;
            document.getElementById('edit-id').value = item.id;
            document.getElementById('edit-nombre').value = item.nombre;
            document.getElementById('edit-categoria').value = item.categoria;
            document.getElementById('edit-licencia').value = item.licencia;
            document.getElementById('edit-so').value = item.so;
            document.getElementById('edit-version').value = item.version;
            document.getElementById('edit-carrera').value = item.carrera;
            document.getElementById('edit-url').value = item.url;
            document.getElementById('edit-winget').value = item.winget || '';
            document.getElementById('edit-descripcion').value = item.desc;
            document.getElementById('modal-editar').classList.remove('hidden');
        }

        function cerrarModalEditar() { document.getElementById('modal-editar').classList.add('hidden'); }

        function renderMisSolicitudes() {
            const cont = document.getElementById('contenedor-mis-solicitudes');
            cont.innerHTML = solicitudes.length === 0 ? `<p class="text-sm text-slate-500">Sin solicitudes registradas.</p>` : solicitudes.map(s => `
                <div class="bg-slate-900 border border-slate-700 rounded-lg px-4 py-3 flex flex-col sm:flex-row items-start sm:items-center justify-between gap-3">
                    <div>
                        <p class="text-sm font-medium text-white">${s.nombre} ${s.estado === 'atendida' ? '<span class="text-xs text-green-400 font-bold"> (¡Atendida!)</span>' : ''}</p>
                        <p class="text-xs text-slate-400">${s.carrera} · ${s.fecha}</p>
                    </div>
                    <span class="text-xs font-semibold px-2.5 py-1 rounded-full ${s.estado === 'pendiente' ? 'bg-yellow-600/20 text-yellow-400' : 'bg-green-600/20 text-green-400'}">${s.estado.toUpperCase()}</span>
                </div>`).join('');
        }

        function renderSolicitudesAdmin() {
            const cont = document.getElementById('contenedor-solicitudes');
            actualizarBadgeSolicitudes();
            cont.innerHTML = solicitudes.length === 0 ? `<p class="text-sm text-slate-500">Sin solicitudes pendientes.</p>` : solicitudes.map(s => `
                <div class="bg-slate-900 border border-slate-700 rounded-lg px-4 py-3">
                    <div class="flex justify-between">
                        <div><p class="text-sm font-semibold text-white">${s.nombre} (${s.carrera})</p><p class="text-sm text-slate-300 mt-1">${s.comentario}</p><p class="text-xs text-slate-500 mt-1">Fecha: ${s.fecha}</p></div>
                        <span class="text-xs px-2 py-1 h-fit rounded-full ${s.estado === 'pendiente' ? 'bg-yellow-600/20 text-yellow-400' : 'bg-green-600/20 text-green-400'}">${s.estado}</span>
                    </div>
                    <div class="mt-3 flex gap-2">
                        ${s.estado === 'pendiente' ? `<button onclick="atenderSolicitud(${s.id})" class="text-xs bg-green-600 text-white px-3 py-1.5 rounded-lg flex items-center gap-1"><i data-lucide="check" class="w-3.5 h-3.5"></i> Marcar Atendida</button>` : ''}
                        <button onclick="eliminarSolicitud(${s.id})" class="text-xs bg-red-600 text-white px-3 py-1.5 rounded-lg">Eliminar</button>
                    </div>
                </div>`).join('');
            lucide.createIcons();
        }

        function actualizarBadgeSolicitudes() {
            const badge = document.getElementById('badge-solicitudes');
            const pendientes = solicitudes.filter(s => s.estado === 'pendiente').length;
            badge.textContent = pendientes;
            pendientes > 0 ? badge.classList.remove('hidden') : badge.classList.add('hidden');
        }

        function renderRecomendaciones() {
            const car = document.getElementById('select-carrera').value;
            const res = softwareBD.filter(s => s.carrera === car);
            document.getElementById('contenedor-recomendaciones').innerHTML = res.map(item => `
                <div class="bg-slate-900 border border-slate-700 p-4 rounded-lg flex justify-between gap-3">
                    <div><h4 class="font-bold text-blue-400">${item.nombre}</h4><p class="text-xs text-slate-300">${item.desc}</p></div>
                    <button onclick="instalarPrograma(${item.id})" class="bg-green-700 text-white p-2 rounded-lg h-fit"><i data-lucide="download" class="w-4 h-4"></i></button>
                </div>`).join('');
            lucide.createIcons();
        }

        function renderFavoritos() {
            const res = softwareBD.filter(s => favoritos.includes(s.id));
            document.getElementById('contenedor-favoritos').innerHTML = res.map(item => `
                <div class="bg-slate-900 border border-slate-700 p-4 rounded-lg flex justify-between items-center">
                    <div><h4 class="font-bold text-white">${item.nombre}</h4></div>
                    <div class="flex items-center gap-3"><button onclick="instalarPrograma(${item.id})" class="bg-green-700 text-white p-2 rounded-lg"><i data-lucide="download" class="w-3.5 h-3.5"></i></button><button onclick="toggleFavorito(${item.id})" class="text-xs text-yellow-500">Quitar</button></div>
                </div>`).join('');
            lucide.createIcons();
        }

        function renderGenerator() {
            const disponibles = softwareBD.filter(s => s.winget && s.winget.length > 0);

            let htmlKits = '';
            const carreras = [...new Set(disponibles.map(s => s.carrera))];
            htmlKits += carreras.map(c => `<button onclick="seleccionarKitCarrera('${c}')" class="bg-slate-900 border border-slate-700 rounded-lg p-4 font-bold text-white text-sm hover:border-blue-500 transition text-left">Kit ${c}</button>`).join('');

            kitsBD.forEach(k => {
                htmlKits += `<button onclick='seleccionarKitPersonalizado(${k.programas})' class="bg-blue-950/40 border border-blue-700/50 rounded-lg p-4 font-bold text-white text-sm hover:border-blue-400 transition text-left">⭐ ${k.nombre} <span class="block text-[10px] text-blue-300 font-normal">(${k.carrera})</span></button>`;
            });

            document.getElementById('contenedor-kits').innerHTML = htmlKits;

            document.getElementById('contenedor-checklist-winget').innerHTML = disponibles.map(item => `
                <label class="flex items-center gap-3 bg-slate-800 p-2 rounded border border-slate-700/50 cursor-pointer"><input type="checkbox" value="${item.winget}" data-carrera="${item.carrera}" onchange="actualizarScriptBat()" class="winget-checkbox"><span class="text-sm text-slate-200">${item.nombre}</span></label>
            `).join('');
            actualizarScriptBat();
        }

        function seleccionarKitCarrera(carrera) {
            document.querySelectorAll('.winget-checkbox').forEach(chk => chk.checked = (chk.dataset.carrera === carrera));
            actualizarScriptBat();
        }

        function seleccionarKitPersonalizado(arrayWingets) {
            const ids = Array.isArray(arrayWingets) ? arrayWingets : [arrayWingets];
            document.querySelectorAll('.winget-checkbox').forEach(chk => {
                chk.checked = ids.includes(chk.value);
            });
            actualizarScriptBat();
        }

        function limpiarSeleccionKit() { document.querySelectorAll('.winget-checkbox').forEach(chk => chk.checked = false); actualizarScriptBat(); }

        function actualizarScriptBat() {
            const checks = document.querySelectorAll('.winget-checkbox:checked');
            let script = "@echo off\n";
            checks.forEach(chk => script += `winget install --id ${chk.value} -e\n`);
            document.getElementById('codigo-bat').textContent = checks.length === 0 ? "Selecciona un kit..." : script;
        }

        function descargarScriptBat() {
            const blob = new Blob([document.getElementById('codigo-bat').textContent], { type: 'text/plain' });
            const enlace = document.createElement('a'); enlace.href = URL.createObjectURL(blob); enlace.download = 'instalar.bat'; enlace.click();
        }

        function renderVerificador() {
            if (rolActual !== 'admin') return;
            document.getElementById('contenedor-verificador').innerHTML = softwareBD.map(item => `
                <div class="bg-slate-900 border border-slate-700 p-3 rounded-lg flex justify-between items-center gap-2">
                    <div><span class="font-bold text-white text-sm">${item.nombre}</span> <span class="text-xs text-slate-400">(${item.carrera})</span></div>
                    <div class="flex gap-2">
                        <button onclick="abrirModalEditar(${item.id})" class="bg-slate-800 text-blue-300 text-xs px-3 py-1.5 rounded-lg border border-slate-700">Editar</button>
                        <button onclick="eliminarSoftware(${item.id})" class="bg-red-950/60 text-red-300 text-xs px-3 py-1.5 rounded-lg border border-red-900">Eliminar</button>
                    </div>
                </div>`).join('');
            lucide.createIcons();
        }

        document.addEventListener("DOMContentLoaded", () => {
            try {
                lucide.createIcons();
                cargarDatosDesdeAPI();
            } catch (e) {
                console.error(e);
            }

            setTimeout(() => {
                const splash = document.getElementById('splash-screen');
                if (splash) {
                    splash.classList.add('splash-oculto');
                    setTimeout(() => {
                        splash.classList.add('hidden');

                        const rolGuardado = localStorage.getItem('softcampus_rol');
                        const seccionGuardada = localStorage.getItem('softcampus_seccion');

                        if (rolGuardado) {
                            rolActual = rolGuardado;
                            if (rolGuardado === 'admin') {
                                document.getElementById('usuario-nombre').textContent = 'Administrador';
                                document.getElementById('usuario-rol').textContent = correoAdminValido;
                            } else {
                                document.getElementById('usuario-nombre').textContent = 'Estudiante';
                                document.getElementById('usuario-rol').textContent = 'Modo Consulta & Kits';
                            }
                            iniciarSesionExitosa(seccionGuardada);
                        } else {
                            document.getElementById('login-container').classList.remove('hidden');
                        }
                    }, 500);
                }
            }, 1200);
        });
    </script>
</body>
</html>


